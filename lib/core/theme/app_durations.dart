/// Centralized animation and timer duration constants.
class AppDurations {
  static const Duration instant = Duration(milliseconds: 1);
  static const Duration extraFast = Duration(milliseconds: 150);
  static const Duration fast = Duration(milliseconds: 200);
  static const Duration normal = Duration(milliseconds: 300);
  static const Duration medium = Duration(milliseconds: 400);
  static const Duration slow = Duration(milliseconds: 600);
  static const Duration extraSlow = Duration(milliseconds: 1000);
  static const Duration splashDelay = Duration(milliseconds: 2000);
}
